export type UserReturn = {
  name: string;
  email: string;
  avatar: string;
  id: string;
};

export type UserPut = {
  name: string;
  avatar: string;
};

export type UserLogged = {
  name: string;
  email: string;
  avatar: string;
  id: string;
};
